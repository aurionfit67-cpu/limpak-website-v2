<?xml version="1.0" encoding="utf-8"?>
<!-- LIMEPAK Android Manifest -->
<!-- Generated by Capacitor -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.limepak.app">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Network security configuration for Android 9+ -->
    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="34">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            
            <!-- Handle web intents -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="limepak.vercel.app" />
                <data android:scheme="http" android:host="limepak.vercel.app" />
            </intent-filter>
            
            <!-- Handle custom scheme -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="limepak" />
            </intent-filter>
        </activity>

        <!-- Capacitor Provider -->
        <provider
            android:name="com.getcapacitor.CapacitorProvider"
            android:authorities="${applicationId}.capacitor.CapacitorProvider"
            android:exported="false" />

        <!-- Capacitor File Provider -->
        <provider
            android:name="com.getcapacitor.file.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Capacitor App Info -->
        <meta-data
            android:name="com.getcapacitor.appName"
            android:value="@string/app_name" />
        <meta-data
            android:name="com.getcapacitor.appId"
            android:value="${applicationId}" />
        <meta-data
            android:name="com.getcapacitor.appVersion"
            android:value="@string/app_version" />
        <meta-data
            android:name="com.getcapacitor.appBuild"
            android:value="@string/app_build" />
    </application>

</manifest>

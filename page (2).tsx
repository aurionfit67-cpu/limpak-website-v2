import { COMPANY, APK_CONFIG } from '@/config/company';

export const metadata = { title: `Download — ${COMPANY.name}`, description: 'Download the LIMEPAK Android application.' };

export default function DownloadPage() { return <div className="py-16 md:py-24"><div className="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center"><p className="text-sm font-semibold text-lime-600 dark:text-lime-400 uppercase tracking-wider mb-3">Download</p><h1 className="text-4xl md:text-6xl font-bold text-gray-900 dark:text-white mb-5">LIMEPAK for Android</h1><p className="text-lg text-gray-600 dark:text-gray-300 mb-8">Version {APK_CONFIG.version}. {APK_CONFIG.releaseStatus}.</p>{APK_CONFIG.enabled && APK_CONFIG.downloadUrl ? <a href={APK_CONFIG.downloadUrl} className="inline-flex items-center justify-center rounded-xl bg-lime-500 px-7 py-3.5 font-medium text-gray-900 hover:bg-lime-400">Download APK</a> : <div className="rounded-2xl border border-lime-500/20 bg-lime-500/5 p-8"><p className="font-semibold text-gray-900 dark:text-white">The Android build is not published yet.</p><p className="text-gray-600 dark:text-gray-300 mt-2">The download page is live and will automatically show the APK link once a release is configured.</p></div>}</div></div>; }
